package Map;
/*
一、请简述Map 的特点。
    Map每个元素由键与值两部分组成
    Map键不能重复,每个键对应一个值
    键和值可以为null


二、说出Entry键值对对象遍历Map集合的原理。
Map中存放的是两种对象，一种称为key(键)，一种称为value(值)，
它们在在Map中是一一对应关系，这一对对象又称做Map 中的一个Entry(项)。
Entry将键值对的对应关系封装成了对象。
即键值对对象，这样我们在遍历Map集合时，就可以从每一个键值对（Entry）对象中获取对应的键与对应的值。
 */
public class demo01Ex01 {
}
